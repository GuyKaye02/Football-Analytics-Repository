# Expected Danger (xD) Models

This folder contains implementations of Expected Danger (xD) models that estimate
how much each pass increases the probability of scoring within the next 15 seconds.

The project includes:
- A baseline model following the Soccermatics course specification
- A regularized, cross-validated logistic framework with richer contextual features

➡️ See subfolders for details.
